## 0.1.0

- Initial release
- Added framework-agnostic `X402Request` abstraction
- Added `PaymentVerifier` for orchestrating scheme verification
- Added `PaymentSchemeVerifier` interface
- Added `VerificationResult` model
- Included core unit tests for verification behavior
